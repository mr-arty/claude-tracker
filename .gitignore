node_modules/
*.log
.DS_Store

# Real session ids and ticket keys. Never commit.
fixtures.local.json

# Tooling scratch, created by gstack. Currently empty, but anything it writes
# here is local state, not source.
.gstack/

#
claude-sessions.txt